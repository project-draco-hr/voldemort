def __repr__(self):
    return (((((((((((('node(id = ' + str(id)) + ', host = ') + host) + ', socket_port = ') + str(socket_port)) + ', http_port = ') + str(http_port)) + ', admin_port = ') + str(admin_port)) + ', partitions = ') + paritions) + ')')
