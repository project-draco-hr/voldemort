{
  for (int i=0; i < 1000; i++) {
    try {
      store.get("test_" + i);
    }
 catch (    UnreachableStoreException e) {
      if (logger.isDebugEnabled())       logger.debug(e,e);
    }
catch (    Exception e) {
      if (logger.isEnabledFor(Level.ERROR))       logger.error(e,e);
    }
  }
}
