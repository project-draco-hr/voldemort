{
  try {
    return Integer.parseInt(getStringProperty(properties,propertyName,defaultValue));
  }
 catch (  NumberFormatException e) {
    throw new IllegalArgumentException("A valid integer must be provided for the config key '" + propertyName + "'. The "+ getName()+ " will not run.",e);
  }
}
