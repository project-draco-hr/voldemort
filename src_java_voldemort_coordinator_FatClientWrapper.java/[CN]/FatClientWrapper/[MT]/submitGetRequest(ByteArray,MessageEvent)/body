{
  try {
    this.fatClientExecutor.submit(new GetRequestExecutor(key,null,getRequest,this.storeClient));
  }
 catch (  RejectedExecutionException rej) {
    handleRejectedException(getRequest);
  }
}
