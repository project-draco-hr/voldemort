{
  try {
    this.fatClientExecutor.submit(new HttpGetRequestExecutor(getRequestObject,getRequestMessageEvent,this.dynamicTimeoutClient));
    if (logger.isDebugEnabled()) {
      logger.debug("Submitted a get request");
    }
  }
 catch (  RejectedExecutionException rej) {
    handleRejectedException(getRequestMessageEvent);
  }
}
