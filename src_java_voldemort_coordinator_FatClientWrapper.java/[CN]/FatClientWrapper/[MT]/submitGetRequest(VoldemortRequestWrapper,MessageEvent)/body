{
  try {
    this.fatClientExecutor.submit(new GetRequestExecutor(getRequestObject,getRequest,this.dynamicTimeoutClient));
  }
 catch (  RejectedExecutionException rej) {
    handleRejectedException(getRequest);
  }
}
