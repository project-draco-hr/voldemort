{
  try {
    this.fatClientExecutor.submit(new PutRequestExecutor(putRequestObject,putRequest,this.dynamicTimeoutClient));
  }
 catch (  RejectedExecutionException rej) {
    handleRejectedException(putRequest);
  }
}
