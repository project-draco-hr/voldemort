{
  try {
    this.fatClientExecutor.submit(new HttpDeleteRequestExecutor(deleteRequestObject,deleteRequestEvent,this.dynamicTimeoutClient));
  }
 catch (  RejectedExecutionException rej) {
    handleRejectedException(deleteRequestEvent);
  }
}
