{
  try {
    this.fatClientExecutor.submit(new HttpDeleteRequestExecutor(deleteRequestObject,deleteRequestEvent,this.dynamicTimeoutClient,startTimestampInNs,this.coordinatorPerfStats));
  }
 catch (  RejectedExecutionException rej) {
    handleRejectedException(rej,deleteRequestEvent);
  }
}
