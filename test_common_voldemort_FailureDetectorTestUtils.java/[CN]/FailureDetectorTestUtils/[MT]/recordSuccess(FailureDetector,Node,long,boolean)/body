{
  ((MutableStoreVerifier)failureDetector.getConfig().getStoreVerifier()).setErrorStore(node,null);
  failureDetector.recordSuccess(node,requestTime);
  if (shouldWait)   failureDetector.waitForAvailability(node);
}
