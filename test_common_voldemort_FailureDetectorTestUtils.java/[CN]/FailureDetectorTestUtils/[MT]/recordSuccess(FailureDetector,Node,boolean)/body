{
  ((MutableStoreResolver)failureDetector.getConfig().getStoreResolver()).setReturnNullStore(node,false);
  failureDetector.recordSuccess(node);
  if (shouldWait)   failureDetector.waitForAvailability(node);
}
