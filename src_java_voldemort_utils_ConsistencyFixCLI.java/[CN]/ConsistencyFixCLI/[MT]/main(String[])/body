{
  Options options=parseArgs(args);
  ConsistencyFix consistencyFix=new ConsistencyFix(options.url,options.storeName,options.progressBar,options.perServerIOPSLimit);
  String summary=consistencyFix.execute(options.parallelism,options.badKeyFileIn,options.badKeyFileOut);
  System.out.println(summary);
}
