{
  if (!"tcp".equals(uri.getScheme()))   throw new IllegalArgumentException("Illegal scheme in bootstrap URL for SocketStoreClientFactory:" + " expected 'tcp' " + "but found '" + uri.getScheme() + "'.");
}
