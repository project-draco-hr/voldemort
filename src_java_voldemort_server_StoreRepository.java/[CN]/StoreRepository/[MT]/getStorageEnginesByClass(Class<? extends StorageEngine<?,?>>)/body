{
  List<StorageEngine<ByteArray,byte[]>> l=new ArrayList<StorageEngine<ByteArray,byte[]>>();
  for (  StorageEngine<ByteArray,byte[]> engine : this.storageEngines.values())   if (engine.getClass().equals(c))   l.add(engine);
  return l;
}
