{
  List<Pair<Integer,Store<ByteArray,byte[]>>> vals=new ArrayList<Pair<Integer,Store<ByteArray,byte[]>>>();
  for (  Map.Entry<Pair<String,Integer>,Store<ByteArray,byte[]>> entry : this.nodeStores.entrySet())   vals.add(Pair.create(entry.getKey().getSecond(),entry.getValue()));
  return vals;
}
