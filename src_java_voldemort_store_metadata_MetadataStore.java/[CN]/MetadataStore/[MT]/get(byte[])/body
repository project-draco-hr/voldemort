{
  List<Versioned<byte[]>> values=Lists.newArrayList();
  for (  Versioned<String> versioned : innerStore.get(ByteUtils.getString(key,"UTF-8")))   values.add(new Versioned<byte[]>(ByteUtils.getBytes(versioned.getValue(),"UTF-8"),versioned.getVersion()));
  return values;
}
