{
  List<Versioned<byte[]>> values=get(new ByteArray(ByteUtils.getBytes(ROLLBACK_CLUSTER_KEY,"UTF-8")));
  if (values.size() > 0) {
    return clusterMapper.readCluster(new StringReader(getSingleValue(values)));
  }
  return null;
}
