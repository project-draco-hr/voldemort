{
  List<RebalancePartitionsInfo> stealInfoList=getRebalancingStealInfoList();
  for (  RebalancePartitionsInfo info : stealInfoList) {
    if (info.getDonorId() == donorId)     return info;
  }
  return null;
}
