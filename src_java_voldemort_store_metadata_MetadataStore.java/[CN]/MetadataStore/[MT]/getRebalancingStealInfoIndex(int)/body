{
  List<RebalancePartitionsInfo> stealInfoList=getRebalancingStealInfoList();
  for (int i=0; i < stealInfoList.size(); i++) {
    if (stealInfoList.get(i).getDonorId() == donorId)     return i;
  }
  return -1;
}
