{
  HashMap<String,RoutingStrategy> routingStrategyMap=createRoutingStrategyMap(cluster,getSystemStoreDefList());
  this.metadataCache.put(SYSTEM_ROUTING_STRATEGY_KEY,new Versioned<Object>(routingStrategyMap));
}
