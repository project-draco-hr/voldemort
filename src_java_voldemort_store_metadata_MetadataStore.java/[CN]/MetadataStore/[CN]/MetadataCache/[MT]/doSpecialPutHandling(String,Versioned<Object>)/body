{
  if (CLUSTER_KEY.equals(key) || STORES_KEY.equals(key)) {
    VectorClock clock=(VectorClock)metadataCache.get(ROUTING_STRATEGY_KEY).getVersion();
    metadataCache.put(ROUTING_STRATEGY_KEY,new Versioned<Object>(updateRoutingStrategies(),clock.incremented(getNodeId(),1)));
  }
  return value;
}
