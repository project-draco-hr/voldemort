{
  this.directory=directory;
  this.storeMapper=new StoreDefinitionsMapper();
  this.clusterMapper=new ClusterMapper();
  if (this.directory.listFiles() == null)   throw new IllegalArgumentException("No configuration found in " + this.directory.getAbsolutePath() + ".");
  if (!this.directory.exists() && this.directory.canRead())   throw new IllegalArgumentException("Metadata directory " + this.directory.getAbsolutePath() + " does not exist or can not be read.");
}
