{
  List<Integer> nodeBeingReplaced=new ArrayList<Integer>();
  nodeBeingReplaced.add(nodeId);
  newAdminClient.metadataMgmtOps.updateRemoteMetadata(nodeBeingReplaced,MetadataStore.SLOP_STREAMING_ENABLED_KEY,Boolean.toString(true));
}
