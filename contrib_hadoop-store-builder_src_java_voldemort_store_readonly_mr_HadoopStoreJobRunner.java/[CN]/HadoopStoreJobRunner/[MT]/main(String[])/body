{
  int res=1;
  try {
    res=ToolRunner.run(new Configuration(),new HadoopStoreJobRunner(),args);
  }
 catch (  Exception e) {
    e.printStackTrace();
    System.err.print("\nTry '--help' for more information.");
    System.exit(1);
  }
  System.exit(res);
}
