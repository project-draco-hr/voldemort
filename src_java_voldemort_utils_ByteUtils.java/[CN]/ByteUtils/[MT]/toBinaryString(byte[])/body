{
  StringBuffer buffer=new StringBuffer();
  for (  byte b : bytes) {
    String bin=Integer.toBinaryString(b);
    for (int i=0; i < 8 - bin.length(); i++)     buffer.append('0');
    buffer.append(bin.substring(Math.max(0,bin.length() - 8)));
  }
  return buffer.toString();
}
