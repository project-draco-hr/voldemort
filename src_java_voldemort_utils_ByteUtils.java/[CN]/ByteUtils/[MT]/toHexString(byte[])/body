{
  StringBuffer buffer=new StringBuffer();
  for (  byte b : bytes) {
    if (Math.abs(b) < 16)     buffer.append('0');
    String hex=Integer.toHexString(b);
    buffer.append(hex.substring(Math.max(0,hex.length() - 2)));
  }
  return buffer.toString();
}
