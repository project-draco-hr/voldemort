{
  if (tempDir == null)   this.tempDir=new File(System.getProperty("java.io.tmpdir"),"hdfs-fetcher");
 else   this.tempDir=Utils.notNull(new File(tempDir,"hdfs-fetcher"));
  this.maxBytesPerSecond=maxBytesPerSecond;
  this.tempDir.mkdirs();
}
