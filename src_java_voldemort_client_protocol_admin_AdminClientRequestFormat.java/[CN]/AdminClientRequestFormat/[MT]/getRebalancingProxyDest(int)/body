{
  Versioned<byte[]> value=doGetRemoteMetadata(rebalancingNodeId,new ByteArray(ByteUtils.getBytes(MetadataStore.REBALANCING_PROXY_DEST_KEY,"UTF-8")));
  return new Versioned<Integer>(Integer.parseInt(ByteUtils.getString(value.getValue(),"UTF-8")),value.getVersion());
}
