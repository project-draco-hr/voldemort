{
  VectorClock oldClock=(VectorClock)getRebalancingProxyDest(rebalancingNodeId).getVersion();
  updateRemoteMetadata(rebalancingNodeId,MetadataStore.REBALANCING_SLAVES_LIST_KEY,new Versioned<String>("" + proxyDestNodeId,oldClock.incremented(rebalancingNodeId,1)));
}
