{
  VectorClock oldClock=(VectorClock)getServerState(nodeId).getVersion();
  doUpdateRemoteMetadata(nodeId,new ByteArray(ByteUtils.getBytes(MetadataStore.SERVER_STATE_KEY,"UTF-8")),new Versioned<byte[]>(ByteUtils.getBytes(state.toString(),"UTF-8"),oldClock.incremented(nodeId,1)));
}
