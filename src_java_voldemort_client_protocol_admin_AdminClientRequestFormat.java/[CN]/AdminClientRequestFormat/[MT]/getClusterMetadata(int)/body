{
  Versioned<byte[]> value=doGetRemoteMetadata(nodeId,new ByteArray(ByteUtils.getBytes(MetadataStore.CLUSTER_KEY,"UTF-8")));
  Cluster cluster=clusterMapper.readCluster(new StringReader(ByteUtils.getString(value.getValue(),"UTF-8")));
  return new Versioned<Cluster>(cluster,value.getVersion());
}
