{
  while (!getStatus().isComplete()) {
    try {
      adminClient.updateEntries(nodeId,storeName,nodeIterator,null,5000,1000,new Runnable(){
        public void run(){
          nodeIterator.purge();
        }
      }
);
      logger.info("slave finished...");
      setCompletion(true,false);
    }
 catch (    VoldemortException e) {
      if (e.getCause() instanceof IOException) {
        nodeIterator.setRecoveryMode();
        logger.error("Exception received while pushing entries to remote node " + nodeId + ". Will retry again after 10000 seconds");
        logger.error(e.getCause());
        Thread.sleep(10000);
      }
 else {
        throw e;
      }
    }
  }
}
