{
  while (!getStatus().isComplete()) {
    try {
      logger.info("Waiting for the completion, with 10s timeout, of pusher slave for " + getStatus().getDescription() + " with id="+ getStatus().getId());
      wait(10000);
    }
 catch (    InterruptedException e) {
    }
  }
}
