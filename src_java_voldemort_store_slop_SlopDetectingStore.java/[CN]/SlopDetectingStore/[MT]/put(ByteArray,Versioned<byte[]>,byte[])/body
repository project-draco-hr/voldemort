{
  if (isLocal(key)) {
    getInnerStore().put(key,value,transforms);
  }
 else {
    Slop slop=new Slop(getName(),Slop.Operation.PUT,key,value.getValue(),transforms,localNode.getId(),new Date());
    slopStore.put(slop.makeKey(),new Versioned<Slop>(slop,value.getVersion()),null);
  }
}
