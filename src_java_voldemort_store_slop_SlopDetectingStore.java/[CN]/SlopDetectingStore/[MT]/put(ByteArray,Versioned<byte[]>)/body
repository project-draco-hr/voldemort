{
  if (isLocal(key)) {
    getInnerStore().put(key,value);
  }
 else {
    Slop slop=new Slop(getName(),Slop.Operation.PUT,key,value.getValue(),localNode.getId(),new Date());
    slopStore.put(slop.makeKey(),new Versioned<Slop>(slop,value.getVersion()));
  }
}
