{
  if (isLocal(key)) {
    return getInnerStore().delete(key,version);
  }
 else {
    Slop slop=new Slop(getName(),Slop.Operation.DELETE,key,null,localNode.getId(),new Date());
    slopStore.put(slop.makeKey(),new Versioned<Slop>(slop,version));
    return false;
  }
}
