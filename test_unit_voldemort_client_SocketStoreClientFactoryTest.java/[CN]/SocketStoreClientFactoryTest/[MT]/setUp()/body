{
  super.setUp();
  socketService=ServerTestUtils.getSocketService(getClusterXml(),getStoreDefXml(),getValidStoreName(),getLocalNode().getSocketPort(),RequestFormatType.VOLDEMORT);
  socketService.start();
}
