{
  SocketStoreClientFactory factory1=new SocketStoreClientFactory(new ClientConfig().setBootstrapUrls(bootstrapUrls).setEnableLazy(useLazy).setFetchAllStoresXmlInBootstrap(fetchAllStoresXml).setSerializerFactory(factory));
  allFactories.add(factory1);
  return factory1;
}
