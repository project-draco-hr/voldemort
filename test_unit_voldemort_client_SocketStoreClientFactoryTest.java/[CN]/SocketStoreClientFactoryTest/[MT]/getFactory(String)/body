{
  StoreClientFactory factory=new SocketStoreClientFactory(new ClientConfig().setBootstrapUrls(bootstrapUrls).setEnableLazy(useLazy).setFetchAllStoresXmlInBootstrap(fetchAllStoresXml));
  allFactories.add(factory);
  return factory;
}
