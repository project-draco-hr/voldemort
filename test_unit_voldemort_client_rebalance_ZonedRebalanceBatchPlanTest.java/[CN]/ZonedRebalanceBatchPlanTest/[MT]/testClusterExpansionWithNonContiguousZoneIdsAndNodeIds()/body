{
  List<RebalanceTaskInfo> batchPlan;
  batchPlan=ClusterTestUtils.getBatchPlan(z1z3ClusterExpansionNN,z1z3ClusterExpansionPP,z1z3Stores);
  assertTrue(batchPlan.size() > 0);
  batchPlan=ClusterTestUtils.getBatchPlan(z1z3z5ClusterExpansionNNN,z1z3z5ClusterExpansionPPP,z1z3z5Stores);
  assertTrue(batchPlan.size() > 0);
}
