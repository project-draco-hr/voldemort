{
  try {
    failureDetectorConfig.setTime(new MockTime(0));
    long delta=test.run(failureDetectorConfig);
    System.out.println(test.getClass().getSimpleName() + ", " + Class.forName(failureDetectorConfig.getImplementationClassName()).getSimpleName()+ ", "+ delta);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
