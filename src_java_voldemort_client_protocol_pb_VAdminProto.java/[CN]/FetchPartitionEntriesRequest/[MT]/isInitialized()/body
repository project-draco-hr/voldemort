{
  if (!hasType)   return false;
  if (!hasStore)   return false;
  if (hasContinue()) {
    if (!getContinue().isInitialized())     return false;
  }
  if (hasStart()) {
    if (!getStart().isInitialized())     return false;
  }
  return true;
}
