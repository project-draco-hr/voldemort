{
  if (hasType()) {
    output.writeEnum(1,getType().getNumber());
  }
  if (hasStore()) {
    output.writeString(2,getStore());
  }
  if (hasContinue()) {
    output.writeMessage(3,getContinue());
  }
  if (hasStart()) {
    output.writeMessage(4,getStart());
  }
  getUnknownFields().writeTo(output);
}
