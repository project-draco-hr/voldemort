{
  for (  voldemort.client.protocol.pb.VAdminProto.ROStoreVersionMap element : getRoStoreVersionsList()) {
    if (!element.isInitialized())     return false;
  }
  if (hasError()) {
    if (!getError().isInitialized())     return false;
  }
  return true;
}
