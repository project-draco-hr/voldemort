{
  getSerializedSize();
  for (  int element : getNodeIdsList()) {
    output.writeInt32(1,element);
  }
  if (hasZoneId()) {
    output.writeInt32(2,getZoneId());
  }
  for (  java.lang.String element : getStoreNamesList()) {
    output.writeString(3,element);
  }
  getUnknownFields().writeTo(output);
}
