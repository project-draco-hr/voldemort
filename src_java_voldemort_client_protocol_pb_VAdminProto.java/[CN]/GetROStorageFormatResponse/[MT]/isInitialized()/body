{
  for (  voldemort.client.protocol.pb.VAdminProto.ROStorageFormatMap element : getRoStorageFormatList()) {
    if (!element.isInitialized())     return false;
  }
  if (hasError()) {
    if (!getError().isInitialized())     return false;
  }
  return true;
}
