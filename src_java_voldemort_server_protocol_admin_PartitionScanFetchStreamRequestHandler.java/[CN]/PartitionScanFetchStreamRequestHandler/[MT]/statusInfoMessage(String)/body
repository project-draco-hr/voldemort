{
  if (logger.isInfoEnabled()) {
    logger.info(tag + " : [partition: " + currentPartition+ ", replica type:"+ currentReplicaType+ ", partitionFetched: "+ currentPartitionFetched+ "] for store "+ storageEngine.getName());
  }
}
