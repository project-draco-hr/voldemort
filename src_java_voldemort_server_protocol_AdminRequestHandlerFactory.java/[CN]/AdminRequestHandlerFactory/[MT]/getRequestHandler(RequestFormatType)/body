{
switch (type) {
case ADMIN_PROTOCOL_BUFFERS:
    return new ProtoBuffAdminServiceRequestHandler(new ErrorCodeMapper(),repository,metadata,voldemortConfig,asyncRunner);
default :
  throw new VoldemortException("Unknown wire format " + type);
}
}
