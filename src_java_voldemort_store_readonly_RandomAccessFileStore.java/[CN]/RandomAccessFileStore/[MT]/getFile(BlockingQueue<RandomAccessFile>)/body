{
  RandomAccessFile file=files.poll(fdWaitTimeoutMs,TimeUnit.MILLISECONDS);
  if (file == null)   throw new VoldemortException("Timeout after waiting for " + fdWaitTimeoutMs + " ms to acquire file descriptor");
 else   return file;
}
