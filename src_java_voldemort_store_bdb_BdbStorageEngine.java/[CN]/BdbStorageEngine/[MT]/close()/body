{
  try {
    if (this.isOpen.compareAndSet(true,false))     this.bdbDatabase.close();
  }
 catch (  DatabaseException e) {
    logger.error(e);
    throw new PersistenceFailureException("Shutdown failed.",e);
  }
}
