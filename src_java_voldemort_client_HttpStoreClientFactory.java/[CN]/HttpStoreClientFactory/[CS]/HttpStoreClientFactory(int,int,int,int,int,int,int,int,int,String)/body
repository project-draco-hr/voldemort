{
  this(numThreads,connectionManagerTimeoutMs,socketSoTimeoutMs,numRetries,connectionTimeoutMs,routingTimeoutMs,nodeBannageMs,maxConnections,maxSingleHostConnections,new DefaultSerializerFactory(),bootstrapUrls);
}
