{
  VectorClock oldClock=(VectorClock)getRemoteClusterState(nodeId).getVersion();
  updateRemoteMetadata(nodeId,MetadataStore.CLUSTER_STATE_KEY,new Versioned<String>(state.toString(),oldClock.incremented(nodeId,1)));
}
