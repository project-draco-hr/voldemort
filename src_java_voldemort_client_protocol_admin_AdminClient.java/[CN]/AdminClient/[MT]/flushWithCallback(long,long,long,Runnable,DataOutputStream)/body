{
  boolean flushed=false;
  if (flushTimer <= System.currentTimeMillis() || 0 == entryCounter % flushCount) {
    outputStream.flush();
    if (null != callback) {
      callback.run();
    }
    flushed=true;
  }
  return flushed;
}
