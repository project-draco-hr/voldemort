{
  boolean flushed=false;
  if (flushTimer <= System.currentTimeMillis() || entryCounter >= flushCount) {
    outputStream.flush();
    if (null != callback) {
      callback.run();
    }
    flushed=true;
  }
  return flushed;
}
