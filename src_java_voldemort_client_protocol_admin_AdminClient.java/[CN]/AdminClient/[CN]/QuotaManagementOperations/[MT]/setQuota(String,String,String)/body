{
  quotaSysStoreClient.putSysStore(QuotaUtils.makeQuotaKey(storeName,QuotaType.valueOf(quotaType)),quotaValue);
  logger.info("Set quota " + quotaType + " to "+ quotaValue+ " for store "+ storeName);
}
