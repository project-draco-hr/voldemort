{
  if (files != null) {
    for (    File f : files) {
      if (f.isDirectory()) {
        rm(Arrays.asList(f.listFiles()));
        f.delete();
      }
 else {
        f.delete();
      }
    }
  }
}
