{
  BufferedReader reader=new BufferedReader(new FileReader(f));
  StringBuilder builder=new StringBuilder();
  for (int curr=reader.read(); curr >= 0; curr=reader.read())   builder.append((char)curr);
  reader.close();
  return builder.toString();
}
