{
  super.setUp();
  int httpPort=8080;
  url="http://localhost:" + httpPort;
  context=ServerTestUtils.getJettyServer(getClusterXml(),getStoreDefXml(),getValidStoreName(),httpPort);
  server=context.getServer();
  httpStore=ServerTestUtils.getHttpStore(getValidStoreName(),httpPort);
}
