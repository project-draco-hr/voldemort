{
  super.setUp();
  context=ServerTestUtils.getJettyServer(getClusterXml(),getStoreDefXml(),getValidStoreName(),getLocalNode().getHttpPort());
  server=context.getServer();
  httpStore=ServerTestUtils.getHttpStore(getValidStoreName(),getLocalNode().getHttpPort());
  url=getLocalNode().getHttpUrl().toString();
}
