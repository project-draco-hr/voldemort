{
  int result=stealerId;
  result=31 * result + donorId;
  result=31 * result + initialCluster.hashCode();
  result=31 * result + (storeToReplicaToAddPartitionList != null ? storeToReplicaToAddPartitionList.hashCode() : 0);
  result=31 * result + (storeToReplicaToDeletePartitionList != null ? storeToReplicaToDeletePartitionList.hashCode() : 0);
  result=31 * result + attempt;
  return result;
}
