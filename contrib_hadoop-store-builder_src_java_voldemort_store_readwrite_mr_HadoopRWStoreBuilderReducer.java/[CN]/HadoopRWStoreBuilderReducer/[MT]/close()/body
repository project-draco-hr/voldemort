{
  this.client.stop();
  logger.info("Finished partition " + this.nodeId + " and chunkId "+ this.chunkId+ " - "+ ((double)(this.totalBytes * 1000.0) / (double)(System.nanoTime() - this.transferStartTime))+ " MBps");
}
