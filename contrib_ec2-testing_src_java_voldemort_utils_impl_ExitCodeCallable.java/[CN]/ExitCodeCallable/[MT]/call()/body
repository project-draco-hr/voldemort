{
  int exitCode=command.execute(commandOutputListener);
  if (exitCode != 0)   throw new Exception("Process on " + command.getHostName() + " exited with code "+ exitCode+ ". Please check the logs for details.");
  return null;
}
