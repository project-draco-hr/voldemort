{
  return "Operation PUT " + ClientRequestHandler.getDebugMessageForKey(key) + " ValueHash"+ (value == null ? "null" : value.hashCode())+ " ClockSize "+ " ValueSize "+ (value == null ? "null" : value.length)+ +clock.sizeInBytes();
}
