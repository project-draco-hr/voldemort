{
  try {
    int index=file.getName().lastIndexOf('-');
    if (index <= 0)     return new VectorClock();
 else     return new VectorClock(Hex.decodeHex(file.getName().substring(index + 1).toCharArray()));
  }
 catch (  DecoderException e) {
    throw new VoldemortException(e);
  }
}
