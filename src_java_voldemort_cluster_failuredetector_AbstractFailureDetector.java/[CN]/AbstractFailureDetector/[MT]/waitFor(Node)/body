{
  NodeStatus nodeStatus=getNodeStatus(node);
synchronized (nodeStatus) {
    while (!isAvailable(nodeStatus)) {
      if (logger.isInfoEnabled())       logger.info("Waiting for node " + node);
      nodeStatus.wait();
    }
  }
}
