{
  NodeStatus nodeStatus=getNodeStatus(node);
synchronized (nodeStatus) {
    boolean previouslyAvailable=nodeStatus.isAvailable();
    nodeStatus.setAvailable(true);
    if (!previouslyAvailable)     notifyAvailable(node);
  }
}
