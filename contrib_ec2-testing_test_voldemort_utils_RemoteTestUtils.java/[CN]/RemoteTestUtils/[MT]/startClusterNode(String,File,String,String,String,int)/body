{
  Map<String,Integer> nodeIds=new HashMap<String,Integer>();
  nodeIds.put(hostName,nodeId);
  startClusterAsync(new ArrayList<String>(Arrays.asList(hostName)),sshPrivateKey,hostUserId,voldemortRootDirectory,voldemortHomeDirectory,nodeIds);
}
