{
  try {
    new SshClusterStopper(hostNames,sshPrivateKey,hostUserId,voldemortRootDirectory,true).execute();
  }
 catch (  Exception e) {
    if (logger.isEnabledFor(Level.WARN))     logger.warn(e,e);
  }
}
