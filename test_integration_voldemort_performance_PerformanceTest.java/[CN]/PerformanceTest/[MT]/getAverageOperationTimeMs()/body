{
  if (!hasCompleted)   throw new RuntimeException("Hasn't finished running yet!");
  double sum=0.0;
  for (int i=0; i < this.operationTimes.length; i++)   sum+=this.operationTimes[i];
  sum/=Time.NS_PER_MS;
  return sum / this.operationTimes.length;
}
