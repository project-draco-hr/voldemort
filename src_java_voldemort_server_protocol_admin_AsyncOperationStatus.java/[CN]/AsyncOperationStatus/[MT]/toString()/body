{
  StringBuilder sb=new StringBuilder().append("id=").append(id).append(" description=").append(description).append(" complete=").append(complete.get()).append(" status=").append(status).append(" hasException=").append(hasException());
  return sb.toString();
}
