{
  PerformSerialRequests action=createAction(new PerformSerialRequests(),pipelineData,read,completeEvent);
  action.setKey(key);
  action.setInsufficientSuccessesEvent(insufficientSuccessesEvent);
  action.setStoreRequest(storeRequest);
  return action;
}
