{
  logger.warn("Could not connect to node " + node.getId() + " at "+ node.getHost()+ " marking as unavailable for "+ this.nodeBannageMs+ " ms.");
  logger.debug(e);
  node.getStatus().setUnavailable();
}
