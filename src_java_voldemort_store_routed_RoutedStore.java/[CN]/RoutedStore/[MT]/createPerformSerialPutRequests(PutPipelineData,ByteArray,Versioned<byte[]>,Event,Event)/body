{
  PerformSerialPutRequests action=createWriteAction(new PerformSerialPutRequests(),pipelineData,completeEvent);
  action.setKey(key);
  action.setMasterDeterminedEvent(masterDeterminedEvent);
  action.setVersioned(versioned);
  return action;
}
