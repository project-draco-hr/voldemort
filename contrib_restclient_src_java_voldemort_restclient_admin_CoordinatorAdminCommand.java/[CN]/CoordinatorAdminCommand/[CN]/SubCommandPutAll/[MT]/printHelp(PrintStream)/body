{
  stream.println();
  stream.println("NAME");
  stream.println("  get - ?");
  stream.println();
  stream.println("SYNOPSIS");
  stream.println("  get -u <url-list> -s <store-name-list>");
  stream.println();
  getParser().printHelpOn(stream);
  stream.println();
}
