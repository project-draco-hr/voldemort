{
  List<String> ownerList=new ArrayList<String>();
synchronized (permitOwners) {
    Iterator<String> i=this.permitOwners.keySet().iterator();
    while (i.hasNext())     ownerList.add(i.next());
  }
  return ownerList;
}
