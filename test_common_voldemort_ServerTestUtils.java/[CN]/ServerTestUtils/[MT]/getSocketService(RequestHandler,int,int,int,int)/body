{
  AbstractSocketService socketService=null;
  if (true) {
    socketService=new NioSocketService(requestHandler,port,bufferSize,coreConnections);
  }
 else {
    socketService=new SocketService(requestHandler,port,coreConnections,maxConnections,bufferSize,"client-request-service");
  }
  return socketService;
}
