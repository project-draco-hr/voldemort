{
  return stats.getThroughput(Tracked.DELETE) + stats.getThroughput(Tracked.GET) + stats.getThroughput(Tracked.GET_VERSIONS)+ stats.getThroughput(Tracked.GET_ALL)+ stats.getThroughput(Tracked.PUT);
}
