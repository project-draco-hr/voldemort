{
  this(new ThreadPoolExecutor(coreThreads,maxThreads,10000L,TimeUnit.MILLISECONDS,new LinkedBlockingQueue<Runnable>(maxQueuedRequests),new DaemonThreadFactory("voldemort-client-thread-"),new ThreadPoolExecutor.CallerRunsPolicy()),maxConnectionsPerNode,maxTotalConnections,socketTimeoutMs,routingTimeoutMs,AbstractStoreClientFactory.DEFAULT_NODE_BANNAGE_MS,socketBufferSize,new DefaultSerializerFactory(),bootstrapUrls);
}
