{
  this(new ThreadPoolExecutor(coreThreads,maxThreads,10000L,TimeUnit.MILLISECONDS,new LinkedBlockingQueue<Runnable>(maxQueuedRequests),Executors.defaultThreadFactory(),new ThreadPoolExecutor.CallerRunsPolicy()),maxConnectionsPerNode,maxTotalConnections,socketTimeoutMs,routingTimeoutMs,AbstractStoreClientFactory.DEFAULT_NODE_BANNAGE_MS,new DefaultSerializerFactory(),bootstrapUrls);
}
