{
  super(service,serializerFactory,routingTimeoutMs,defaultNodeBannageMs,boostrapUrls);
  this.socketPool=new SocketPool(maxConnectionsPerNode,maxTotalConnections,socketTimeoutMs,DEFAULT_SOCKET_BUFFER_SIZE);
}
