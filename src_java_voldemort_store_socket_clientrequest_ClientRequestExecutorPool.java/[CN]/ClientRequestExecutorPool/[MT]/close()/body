{
  if (stats != null) {
    try {
      if (this.jmxEnabled)       JmxUtils.unregisterMbean(JmxUtils.createObjectName(JmxUtils.getPackageName(this.getClass()),"aggregated" + this.identifierString));
    }
 catch (    Exception e) {
    }
    stats.close();
  }
  factory.close();
  queuedPool.close();
}
