{
  String tempDir=System.getProperty("java.io.tmpdir") + File.separatorChar + (Math.abs(random.nextInt()) % 1000000);
  File temp=new File(tempDir);
  temp.mkdir();
  temp.deleteOnExit();
  return temp;
}
