{
  super(errorMapper,repository);
  if (protocolVersion < 0 || protocolVersion > 2)   throw new IllegalArgumentException("Unknown protocol version: " + protocolVersion);
  this.protocolVersion=protocolVersion;
}
