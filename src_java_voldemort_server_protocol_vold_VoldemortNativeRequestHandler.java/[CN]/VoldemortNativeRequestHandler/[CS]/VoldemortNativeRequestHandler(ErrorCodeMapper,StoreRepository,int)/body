{
  super(errorMapper,repository);
  if (protocolVersion < 0 || protocolVersion > 1)   throw new IllegalArgumentException("Unknown protocol version: " + protocolVersion);
  this.protocolVersion=protocolVersion;
}
