{
  if (this.config.isDefaultClientEnabled()) {
    return new DefaultStoreClient<K,V>(storeName,resolver,this,3);
  }
  return new ZenStoreClient<K,V>(storeName,resolver,this,3,clientContextName,sequencer.getAndIncrement(),config);
}
