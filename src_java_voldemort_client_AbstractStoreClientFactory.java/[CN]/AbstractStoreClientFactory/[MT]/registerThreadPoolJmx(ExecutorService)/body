{
  try {
    registerJmx(JmxUtils.createObjectName(JmxUtils.getPackageName(threadPool.getClass()),JmxUtils.getClassName(threadPool.getClass()) + jmxId),threadPool);
  }
 catch (  Exception e) {
    logger.error("Error registering threadpool jmx: ",e);
  }
}
