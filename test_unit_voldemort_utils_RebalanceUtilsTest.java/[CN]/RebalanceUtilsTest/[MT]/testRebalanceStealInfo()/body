{
  RebalanceStealInfo info=new RebalanceStealInfo(3,Arrays.asList(1,2,3,4,5),1);
  assertEquals("fromString(toString) should match with original",info.toString(),new RebalanceStealInfo(info.toString()).toString());
  System.out.println(info.toString());
}
