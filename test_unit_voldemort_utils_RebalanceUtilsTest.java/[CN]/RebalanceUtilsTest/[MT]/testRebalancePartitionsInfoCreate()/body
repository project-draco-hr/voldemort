{
  RebalancePartitionsInfo info=new RebalancePartitionsInfo(0,1,Lists.newArrayList(1),Lists.newArrayList(7),Lists.newArrayList(3),Lists.newArrayList("test"),0);
  String jsonString=info.toJsonString();
  RebalancePartitionsInfo info2=RebalancePartitionsInfo.create(jsonString);
  assertEquals(info,info2);
}
