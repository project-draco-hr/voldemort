{
  String schema=conf.get(prop);
  if (schema == null)   throw new IllegalArgumentException("The required property '" + prop + "' is not defined in the JobConf for this Hadoop job.");
  JsonTypeDefinition.fromJson(schema);
  return schema;
}
