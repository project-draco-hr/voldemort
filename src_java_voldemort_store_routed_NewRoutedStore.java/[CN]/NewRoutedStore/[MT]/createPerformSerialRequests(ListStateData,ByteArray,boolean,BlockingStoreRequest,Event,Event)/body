{
  PerformSerialRequests action=createAction(new PerformSerialRequests(),stateData,read,completeEvent);
  action.setKey(key);
  action.setInsufficientSuccessesEvent(insufficientSuccessesEvent);
  action.setStoreRequest(storeRequest);
  return action;
}
