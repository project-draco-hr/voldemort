{
  PerformSerialPutRequests action=createWriteAction(new PerformSerialPutRequests(),stateData,completeEvent);
  action.setKey(key);
  action.setMasterDeterminedEvent(masterDeterminedEvent);
  action.setVersioned(versioned);
  return action;
}
