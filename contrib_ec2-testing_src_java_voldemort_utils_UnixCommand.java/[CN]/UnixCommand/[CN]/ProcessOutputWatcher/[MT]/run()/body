{
  try {
    String line=null;
    while ((line=reader.readLine()) != null)     commandOutputListener.outputReceived(outputType,hostName,line);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
