{
  if (channelEvent instanceof MessageEvent) {
    logger.error("Executing request now ");
    getExecutor().execute(new StorageWorkerThread((MessageEvent)channelEvent,performanceStats,localZoneId));
  }
}
