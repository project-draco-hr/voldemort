{
  long duration=receivedTimeInMs - (this.parsedRequestOriginTimeInMs);
  int numVectorClockEntries=(this.parsedVectorClock == null ? 0 : this.parsedVectorClock.getVersionMap().size());
  logger.debug("Received a " + operationType + " request for key(s): "+ keysHexString(this.parsedKeys)+ " , store: "+ this.storeName+ " , origin time (in ms): "+ (this.parsedRequestOriginTimeInMs)+ " , requested received at time(in ms): "+ receivedTimeInMs+ " , num vector clock entries: "+ numVectorClockEntries+ " , duration from RESTClient to CoordinatorRestRequestValidator(in ms): "+ duration);
}
