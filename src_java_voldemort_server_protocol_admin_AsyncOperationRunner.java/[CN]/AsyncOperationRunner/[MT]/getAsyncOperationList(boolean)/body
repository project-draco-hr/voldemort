{
  Set<Integer> keySet=ImmutableSet.copyOf(operations.keySet());
  if (showComplete)   return new ArrayList<Integer>(keySet);
  List<Integer> keyList=new ArrayList<Integer>();
  for (  int key : keySet) {
    if (!operations.get(key).getStatus().isComplete())     keyList.add(key);
  }
  return keyList;
}
