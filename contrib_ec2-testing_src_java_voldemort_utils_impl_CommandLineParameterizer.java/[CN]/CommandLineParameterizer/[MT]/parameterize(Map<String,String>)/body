{
  String command=rawCommand;
  for (  Map.Entry<String,String> parameter : parameters.entrySet())   command=StringUtils.replace(command,"${" + parameter.getKey() + "}",parameter.getValue());
  return command;
}
