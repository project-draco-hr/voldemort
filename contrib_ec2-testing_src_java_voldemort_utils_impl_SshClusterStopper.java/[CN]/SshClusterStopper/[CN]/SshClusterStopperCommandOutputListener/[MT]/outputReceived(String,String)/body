{
  if (line.contains("Stopping Voldemort...")) {
    completedCounter.incrementAndGet();
    if (logger.isInfoEnabled()) {
      logger.info(hostName + " shutdown complete");
      if (hostCount == completedCounter.get())       logger.info("Cluster shutdown complete");
    }
  }
}
