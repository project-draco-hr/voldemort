{
  int size=din.readInt();
  byte[] data=new byte[size];
  int readLen=din.read(data);
  if (size != readLen) {
    throw new VoldemortException("Unable to read Byte correctly expected size:" + size + " got size:"+ readLen+ " data:"+ new String(data));
  }
  return data;
}
