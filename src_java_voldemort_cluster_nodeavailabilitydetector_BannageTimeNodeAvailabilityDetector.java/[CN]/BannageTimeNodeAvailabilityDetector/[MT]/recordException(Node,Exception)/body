{
  logger.warn("Could not connect to node " + node.getId() + " at "+ node.getHost()+ " marking as unavailable for "+ nodeBannageMs+ " ms.",e);
  logger.debug(e);
  getNodeStatus(node).setUnavailable();
}
