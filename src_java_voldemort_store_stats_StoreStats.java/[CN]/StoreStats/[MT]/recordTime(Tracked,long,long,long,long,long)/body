{
  counters.get(op).addRequest(timeNS,numEmptyResponses,valueSize,keySize,getAllAggregateRequests);
  if (parent != null)   parent.recordTime(op,timeNS,numEmptyResponses,valueSize,keySize,getAllAggregateRequests);
}
