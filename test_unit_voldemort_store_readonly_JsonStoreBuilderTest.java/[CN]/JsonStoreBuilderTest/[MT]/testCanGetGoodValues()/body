{
  int matched=0;
  for (int i=0; i < 3; i++) {
    for (    Map.Entry<String,String> entry : this.data.entrySet()) {
      List<Versioned<Object>> found=this.store.get(entry.getKey());
      if (found.size() > 0) {
        assertEquals("Lookup failure for '" + entry.getKey() + "' on iteration "+ i,1,found.size());
        Versioned<Object> obj=found.get(0);
        assertEquals(entry.getValue(),obj.getValue());
        matched++;
      }
    }
  }
  assertEquals("Approx half keys should be matched.",true,matched > (0.45) * (data.entrySet().size()));
}
