{
  for (int i=0; i < 3; i++) {
    for (int j=0; j < TEST_SIZE; j++) {
      String key=TestUtils.randomLetters(10);
      if (!this.data.containsKey(key))       assertEquals(0,this.store.get(key).size());
    }
  }
}
