{
  Response<K,V> response=(Response<K,V>)eventData;
  pipelineData.incrementCompleted();
  if (logger.isDebugEnabled())   logger.debug("Response received, attempts: " + pipelineData.getAttempts() + ", completed: "+ pipelineData.getCompleted());
  executeInternal(pipeline,response);
}
