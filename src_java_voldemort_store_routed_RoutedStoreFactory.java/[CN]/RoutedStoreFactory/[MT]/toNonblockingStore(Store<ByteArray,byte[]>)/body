{
  if (store instanceof NonblockingStore)   return (NonblockingStore)store;
  if (logger.isEnabledFor(Level.WARN))   logger.warn("Using pseudo NonblockingStore implementation for " + store.getClass());
  return new ThreadPoolBasedNonblockingStoreImpl(threadPool,store);
}
