{
  Element root=new Element(STORES_ELMT);
  for (  StoreDefinition def : stores)   root.addContent(toElement(def));
  XMLOutputter serializer=new XMLOutputter(Format.getPrettyFormat());
  return serializer.outputString(root);
}
