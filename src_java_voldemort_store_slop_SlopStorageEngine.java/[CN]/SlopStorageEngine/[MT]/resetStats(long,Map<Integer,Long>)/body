{
  totalAddedSinceReset.set(0);
  this.totalOutstanding=outstanding;
  for (int i=0; i < numNodes; i++)   addedSinceResetByNode.get(i).set(0);
  for (  Map.Entry<Integer,Long> entry : outstandingByNode.entrySet())   this.outstandingByNode.get(entry.getKey()).set(entry.getValue());
}
