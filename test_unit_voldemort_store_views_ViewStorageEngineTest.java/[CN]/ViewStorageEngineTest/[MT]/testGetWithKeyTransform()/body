{
  target.put("test",Versioned.value("you"));
  assertEquals("Should not find test after tranformation of key",0,keyView.get("test").size());
  assertEquals("Should find test42 after transformation of key","you",keyView.get("test42").get(0).getValue());
}
