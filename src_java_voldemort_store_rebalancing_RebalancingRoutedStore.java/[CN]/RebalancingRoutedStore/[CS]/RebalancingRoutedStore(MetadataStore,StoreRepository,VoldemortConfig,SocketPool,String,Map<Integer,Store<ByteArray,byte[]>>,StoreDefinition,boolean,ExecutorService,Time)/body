{
  super(name,innerStores,metadataStore.getCluster(),storeDef,repairReads,threadPool,voldemortConfig.getRoutingTimeoutMs(),voldemortConfig.getClientNodeBannageMs(),time);
  this.metadata=metadataStore;
  this.storeRepository=storeRepository;
  this.voldemortConfig=voldemortConfig;
  this.socketPool=socketPool;
}
