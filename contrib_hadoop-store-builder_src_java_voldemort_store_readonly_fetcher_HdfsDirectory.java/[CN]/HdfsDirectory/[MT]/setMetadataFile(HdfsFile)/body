{
  if (metadataFile != null) {
    throw new VoldemortApplicationException("more than one metadata file present existing file " + metadataFile.getPath() + " new file "+ file.getPath());
  }
  metadataFile=file;
}
