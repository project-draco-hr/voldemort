{
  adminClient.setAdminClientCluster(targetCluster);
  List<StoreDefinition> storeDefs=RebalanceUtils.getCurrentStoreDefinitions(targetCluster,adminClient);
  rebalance(currentCluster,targetCluster,storeDefs);
}
