{
  if (b == null) {
    output.writeShort(-1);
  }
 else   if (b.length < Short.MAX_VALUE) {
    output.writeShort(b.length);
    output.write(b);
  }
 else {
    throw new SerializationException("Array has length " + b.length + " which is too large to serialize.");
  }
}
