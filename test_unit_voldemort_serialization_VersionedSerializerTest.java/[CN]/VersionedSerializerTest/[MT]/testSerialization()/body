{
  assertSerializes("Empty Versioned should equal itself.",new Versioned<String>("",getClock()));
  assertSerializes("Normal string should equal itself.",new Versioned<String>("hello",getClock()));
  assertSerializes("Normal string should equal itself.",new Versioned<String>("hello",getClock(1,1,1,2,3)));
  assertSerializes("Normal string should equal itself.",new Versioned<String>("hello",getClock(1)));
  assertSerializes("Normal string should equal itself.",new Versioned<String>("hello",null));
}
