{
  logger.trace(preamble + " - position: " + inputStream.getBuffer().position()+ ", limit: "+ inputStream.getBuffer().limit()+ ", remaining: "+ inputStream.getBuffer().remaining()+ ", capacity: "+ inputStream.getBuffer().capacity()+ ", lastCompleteStreamPosition: "+ lastCompleteStreamPosition+ " - for "+ socketChannel.socket().getRemoteSocketAddress());
}
