{
  for (  RemoteTestResult remoteTestResult : remoteTestResults) {
    for (    Map.Entry<Integer,RemoteTestIteration> entry : remoteTestResult.getRemoteTestIterations().entrySet()) {
      int iteration=entry.getKey();
      RemoteTestIteration rti=entry.getValue();
      System.out.println(remoteTestResult.getHostName() + " " + iteration+ " "+ rti.getReadsPerSecond()+ " "+ rti.getWritesPerSecond()+ " "+ rti.getDeletesPerSecond());
    }
  }
}
