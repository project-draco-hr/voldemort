{
  long nowMS=System.currentTimeMillis();
  String message=" Error occured during file download " + file.getName() + " after attempts "+ attempts+ " time elapsed "+ (nowMS - startTimeMS)+ " MS.";
  reportError(message,t);
}
