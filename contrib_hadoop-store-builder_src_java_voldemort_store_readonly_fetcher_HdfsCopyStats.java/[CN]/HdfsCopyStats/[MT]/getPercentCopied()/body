{
  if (pathInfo == null) {
    return -1.0;
  }
  if (pathInfo.getTotalSize() == 0) {
    return 0.0;
  }
 else {
    return (double)(totalBytesTransferred * 100) / (double)pathInfo.getTotalSize();
  }
}
