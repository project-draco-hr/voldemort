{
  if (!RebalanceUtils.containsNode(adminClient.getCluster(),stealerNodeId)) {
    adminClient.setCluster(RebalanceUtils.updateCluster(adminClient.getCluster(),Arrays.asList(targetCluster.getNodeById(stealerNodeId))));
  }
}
