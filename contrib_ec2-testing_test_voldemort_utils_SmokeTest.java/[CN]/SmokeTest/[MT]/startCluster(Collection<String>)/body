{
  ClusterStarter voldemortClusterStarter=new SshClusterStarter();
  voldemortClusterStarter.start(hostNames,hostUserId,sshPrivateKey,voldemortRootDirectory + "/voldemort",voldemortHomeDirectory,600000);
}
