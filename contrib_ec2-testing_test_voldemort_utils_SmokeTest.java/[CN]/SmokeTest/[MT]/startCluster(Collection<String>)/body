{
  VoldemortClusterStarter voldemortClusterStarter=new SshVoldemortClusterStarter();
  voldemortClusterStarter.start(hostNames,hostUserId,sshPrivateKey,voldemortRootDirectory + "/voldemort",voldemortHomeDirectory);
}
