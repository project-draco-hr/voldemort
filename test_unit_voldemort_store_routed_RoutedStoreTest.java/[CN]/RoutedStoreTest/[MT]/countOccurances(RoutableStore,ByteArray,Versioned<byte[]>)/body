{
  int count=0;
  for (  Store<ByteArray,byte[]> store : routableStore.getInnerStores().values())   try {
    if (store.get(key).size() > 0 && Utils.deepEquals(store.get(key).get(0),value))     count+=1;
  }
 catch (  VoldemortException e) {
  }
  return count;
}
