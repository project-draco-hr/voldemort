{
  int count=0;
  for (  Store<byte[],byte[]> store : routedStore.getInnerStores().values())   try {
    if (store.get(key).size() > 0 && Objects.deepEquals(store.get(key).get(0),value))     count+=1;
  }
 catch (  VoldemortException e) {
  }
  return count;
}
