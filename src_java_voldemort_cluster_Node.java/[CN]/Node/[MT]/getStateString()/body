{
  String stateString=briefToString() + " in zone " + getZoneId()+ " with admin port "+ getAdminPort()+ ", socket port "+ getSocketPort()+ ", and http port "+ getHttpPort();
  if (getRestPort() > 0) {
    stateString+=", rest port" + getRestPort();
  }
  return stateString;
}
