{
  logger.info("Successfully scanned " + scanned + " tuples in "+ ((System.currentTimeMillis() - startTime) / 1000)+ " s");
  for (int i=0; i < fetched.length; i++) {
    logger.info(fetched[i] + " tuples fetched for store '" + storeName+ " for node "+ i);
  }
}
