{
  try {
    getInnerStore().put(key,new Versioned<byte[]>(IOUtils.toByteArray(new GZIPInputStream(new ByteArrayInputStream(value.getValue()))),(VectorClock)value.getVersion()));
  }
 catch (  IOException e) {
    throw new VoldemortException(e);
  }
}
