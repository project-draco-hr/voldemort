{
  try {
    SystemStore<String,Long> sysVersionStore=new SystemStore<String,Long>("test-store",bootStrapUrls,this.CLIENT_ZONE_ID,this.clusterXml,null);
    fail("Should not execute this. We can only connect to system store with a 'voldsys$' prefix.");
  }
 catch (  Exception e) {
  }
}
