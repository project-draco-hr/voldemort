{
  for (  RebalancePartitionsInfo info : getAll()) {
    if (info.getUnbalancedStoreList().contains(storeName)) {
      if (StoreRoutingPlan.checkKeyBelongsToPartition(keyPartitions,nodePartitions,info.getReplicaToAddPartitionList(storeName))) {
        return info;
      }
    }
  }
  return null;
}
