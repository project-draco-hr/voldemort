{
  for (  int p : partitionIds) {
    for (    RebalancePartitionsInfo info : getAll()) {
      if (info.getUnbalancedStoreList().contains(store) && info.getPartitionList().contains(p))       return info;
    }
  }
  return null;
}
