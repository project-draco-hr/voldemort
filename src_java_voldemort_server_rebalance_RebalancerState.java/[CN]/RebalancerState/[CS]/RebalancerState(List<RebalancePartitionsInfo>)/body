{
  stealInfoMap=Maps.newHashMapWithExpectedSize(stealInfoList.size());
  for (  RebalancePartitionsInfo rebalancePartitionsInfo : stealInfoList)   stealInfoMap.put(rebalancePartitionsInfo.getDonorId(),rebalancePartitionsInfo);
}
