{
  if (this == null)   return true;
 else   if (object == null)   return false;
 else   if (!object.getClass().equals(Versioned.class))   return false;
  Versioned versioned=(Versioned)object;
  return Objects.equal(getVersion(),versioned.getVersion()) && Objects.deepEquals(getValue(),versioned.getValue());
}
