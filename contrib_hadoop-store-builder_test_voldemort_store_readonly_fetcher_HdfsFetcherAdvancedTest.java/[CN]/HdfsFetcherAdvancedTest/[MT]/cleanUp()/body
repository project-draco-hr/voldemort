{
  if (testSourceDir != null)   deleteDir(testSourceDir);
  if (testDestDir != null)   deleteDir(testDestDir);
  if (testCompressedSourceDir != null)   deleteDir(testCompressedSourceDir);
  source=null;
  checksumCalculated=null;
  fetcher=null;
  fs=null;
  stats=null;
  input=null;
  buffer=null;
}
