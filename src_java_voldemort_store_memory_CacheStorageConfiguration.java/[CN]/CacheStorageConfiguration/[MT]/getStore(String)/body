{
  ReferenceMap<KeyWrapper,List<Versioned<byte[]>>> backingMap=new ReferenceMap<KeyWrapper,List<Versioned<byte[]>>>(ReferenceType.STRONG,ReferenceType.SOFT);
  return new InMemoryStorageEngine<byte[],byte[]>(name,backingMap);
}
