{
  PartitionBalance pb=new PartitionBalance(ClusterTestUtils.getZZCluster(),ClusterTestUtils.getZZStoreDefsInMemory());
  System.out.println(pb);
  pb=new PartitionBalance(ClusterTestUtils.getZZZCluster(),ClusterTestUtils.getZZZStoreDefsInMemory());
  System.out.println(pb);
}
