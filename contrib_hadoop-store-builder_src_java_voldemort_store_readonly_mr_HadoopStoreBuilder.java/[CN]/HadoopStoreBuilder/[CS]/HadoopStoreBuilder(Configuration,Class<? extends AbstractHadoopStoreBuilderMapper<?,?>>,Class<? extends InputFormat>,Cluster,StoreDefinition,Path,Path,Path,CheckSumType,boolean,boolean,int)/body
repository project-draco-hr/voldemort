{
  this(conf,mapperClass,inputFormatClass,cluster,storeDef,-1,tempDir,outputDir,inputPath,checkSumType);
  this.saveKeys=saveKeys;
  this.reducerPerBucket=reducerPerBucket;
  this.numChunks=numChunks;
}
