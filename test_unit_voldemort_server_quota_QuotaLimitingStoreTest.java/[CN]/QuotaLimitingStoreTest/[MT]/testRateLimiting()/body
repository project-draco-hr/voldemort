{
  setQuota(2);
  ensureThrottled();
  setQuota(Integer.MAX_VALUE);
  ensureNotThrottled();
  setQuota(2);
  ensureThrottled();
  disableQuotaEnforcing();
  ensureNotThrottled();
  enableQuotaEnforcing();
  ensureThrottled();
  unSetQuota();
  ensureNotThrottled();
}
