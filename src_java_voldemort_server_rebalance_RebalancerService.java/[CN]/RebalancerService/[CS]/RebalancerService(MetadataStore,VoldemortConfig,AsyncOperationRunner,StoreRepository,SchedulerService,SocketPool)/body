{
  super(ServiceType.REBALANCE);
  schedulerService=service;
  rebalancer=new Rebalancer(metadataStore,voldemortConfig,asyncRunner,storeRepository,socketPool);
  periodMs=voldemortConfig.getRebalancingServicePeriod();
}
