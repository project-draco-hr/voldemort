{
  if (logger.isDebugEnabled()) {
    double elapsedMs=(time.getNanoseconds() - startNs) / (double)Time.NS_PER_MS;
    logger.debug(instanceName + operation + " operation on store '"+ getName()+ "' completed "+ (success ? "successfully" : "unsuccessfully")+ " in "+ elapsedMs+ " ms.");
  }
}
