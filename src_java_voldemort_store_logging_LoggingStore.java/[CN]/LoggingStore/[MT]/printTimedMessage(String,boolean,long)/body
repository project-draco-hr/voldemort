{
  if (logger.isDebugEnabled()) {
    double elapsedMs=(time.getNanoseconds() - startNs) / (double)Time.NS_PER_MS;
    logger.debug(instanceName + operation + " "+ getName()+ " "+ (success ? "successful" : "unsuccessful")+ " in "+ elapsedMs+ " ms");
  }
}
