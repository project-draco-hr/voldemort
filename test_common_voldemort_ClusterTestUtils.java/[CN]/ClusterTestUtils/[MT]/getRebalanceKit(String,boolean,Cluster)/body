{
  return getRebalanceKit(bootstrapUrl,RebalanceController.MAX_PARALLEL_REBALANCING,RebalanceController.MAX_TRIES_REBALANCING,RebalanceController.REBALANCING_CLIENT_TIMEOUT_SEC,stealerBased,finalCluster);
}
