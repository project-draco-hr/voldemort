{
  this.port=port;
  this.threadGroup=new ThreadGroup("voldemort-socket-server");
  this.storeMap=storeMap;
  this.threadPool=new ThreadPoolExecutor(defaultThreads,maxThreads,1,TimeUnit.SECONDS,new SynchronousQueue<Runnable>(),threadFactory,rejectedExecutionHandler);
}
