{
  if (!storeRepository.hasRedirectingSocketStore(storeName,donorNode.getId())) {
    storeRepository.addRedirectingSocketStore(donorNode.getId(),createRedirectingSocketStore(storeName,donorNode));
  }
}
