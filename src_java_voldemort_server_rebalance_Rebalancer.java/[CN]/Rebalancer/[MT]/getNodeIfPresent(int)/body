{
  try {
    return metadataStore.getCluster().getNodeById(donorId);
  }
 catch (  Exception e) {
    throw new VoldemortException("Failed to get donorNode " + donorId + " from current cluster "+ metadataStore.getCluster()+ " at node "+ metadataStore.getNodeId(),e);
  }
}
