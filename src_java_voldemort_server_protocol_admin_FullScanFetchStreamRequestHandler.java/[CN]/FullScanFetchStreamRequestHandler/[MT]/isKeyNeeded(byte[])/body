{
  if (!StoreRoutingPlan.checkKeyBelongsToPartitionIds(nodeId,key,initialCluster,storeDef)) {
    return false;
  }
  if (recordsPerPartition <= 0) {
    return true;
  }
  if (partitionsToFetch.contains(getKeyPartitionId(key))) {
    return true;
  }
  return false;
}
