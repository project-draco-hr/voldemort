{
  if (logger.isInfoEnabled()) {
    logger.info(tag + " : [partition: " + currentPartition+ ", replica type:"+ currentReplicaType+ ", partitionFetched: "+ partitionFetched+ "] for store "+ storageEngine.getName());
  }
}
