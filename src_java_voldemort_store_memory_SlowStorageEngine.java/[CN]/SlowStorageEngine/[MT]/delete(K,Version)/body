{
  if (queueingDelays.deleteMs > 0)   queueingSleep(queueingDelays.deleteMs);
  if (concurrentDelays.deleteMs > 0)   concurrentSleep(concurrentDelays.deleteMs);
  return imStore.delete(key,version);
}
