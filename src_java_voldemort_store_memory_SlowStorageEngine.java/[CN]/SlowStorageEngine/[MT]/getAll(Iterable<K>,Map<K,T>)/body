{
  if (queueingDelays.getAllMs > 0)   queueingSleep(queueingDelays.getAllMs);
  if (concurrentDelays.getAllMs > 0)   concurrentSleep(concurrentDelays.getAllMs);
  return imStore.getAll(keys,transforms);
}
