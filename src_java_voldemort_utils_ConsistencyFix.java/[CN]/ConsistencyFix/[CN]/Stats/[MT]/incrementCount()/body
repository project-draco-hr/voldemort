{
  count++;
  if (count % progressBar == 0) {
    long nowTimeMs=System.currentTimeMillis();
    logger.info("Bad keys attempted to be processed count = " + count + " ("+ getPrettyQPS(progressBar,lastTimeMs - nowTimeMs)+ " keys/second)");
    lastTimeMs=nowTimeMs;
  }
}
