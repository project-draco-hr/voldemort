{
  return MetadataStore.VoldemortState.REBALANCING_MASTER_SERVER.equals(metadata.getServerState()) && metadata.getRebalancingStealInfo().getUnbalancedStoreList().contains(getName()) && checkKeyBelongsToStolenPartitions(key);
}
