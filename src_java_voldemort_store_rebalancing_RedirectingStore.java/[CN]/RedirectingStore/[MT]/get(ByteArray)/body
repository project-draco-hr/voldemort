{
  if (MetadataStore.ServerState.REBALANCING_STEALER_STATE.equals(metadata.getServerState()) && checkKeyBelongsToStolenPartitions(key)) {
    return proxyGet(key);
  }
 else {
    return getInnerStore().get(key);
  }
}
