{
  List<ByteArray> redirectingKeys=new ArrayList<ByteArray>();
  for (  ByteArray key : keys) {
    if (redirectingKey(key))     redirectingKeys.add(key);
  }
  if (!redirectingKeys.isEmpty())   proxyGetAllAndLocalPut(redirectingKeys);
  return getInnerStore().getAll(keys);
}
