{
  for (  int partitionId : metadata.getRoutingStrategy(getName()).getPartitionList(key.get())) {
    if (rebalancePartitionsInfo.getPartitionList().contains(partitionId))     return true;
  }
  return false;
}
