{
  for (  int partitionId : partitionIds) {
    for (    RebalancePartitionsInfo candidate : metadata.getRebalancingStealInfoList()) {
      if (candidate.getUnbalancedStoreList().contains(getName()) && candidate.getPartitionList().contains(partitionId)) {
        return candidate;
      }
    }
  }
  return null;
}
