{
  for (  int nodeId : nodeIds) {
    if (nodeId < NUM_SERVERS) {
      servers[nodeId]=ServerTestUtils.startVoldemortServerInMannerThatMayResultInBindException(socketStoreFactory,configs[nodeId],cluster);
    }
  }
}
