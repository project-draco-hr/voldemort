{
  GetClientRequest clientRequest=new GetClientRequest(STORE_NAME,new RequestFormatFactory().getRequestFormat(dest1.getRequestFormatType()),RequestRoutingType.ROUTED,new ByteArray(new byte[]{1,2,3}),null);
  return clientRequest;
}
