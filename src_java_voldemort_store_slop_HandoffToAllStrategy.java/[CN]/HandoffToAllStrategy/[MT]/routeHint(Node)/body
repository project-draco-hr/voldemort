{
  Collections.shuffle(nodes);
  List<Node> prefList=Lists.newArrayListWithCapacity(nodes.size());
  for (  Node node : nodes) {
    if (node.getId() != origin.getId())     prefList.add(node);
  }
  return prefList;
}
