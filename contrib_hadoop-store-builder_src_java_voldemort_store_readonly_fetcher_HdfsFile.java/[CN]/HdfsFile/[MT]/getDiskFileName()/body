{
  String fileName=getPath().getName();
  if (fileName.endsWith(HdfsFetcher.GZIP_FILE_EXTENSION)) {
    fileName=fileName.substring(0,fileName.length() - HdfsFetcher.GZIP_FILE_EXTENSION.length());
  }
  return fileName;
}
