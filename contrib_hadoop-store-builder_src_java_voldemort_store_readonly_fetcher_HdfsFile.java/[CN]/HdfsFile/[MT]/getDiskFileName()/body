{
  String fileName=getPath().getName();
  if (fileName.endsWith(ReadOnlyUtils.GZIP_FILE_EXTENSION)) {
    fileName=fileName.substring(0,fileName.length() - ReadOnlyUtils.GZIP_FILE_EXTENSION.length());
  }
  return fileName;
}
