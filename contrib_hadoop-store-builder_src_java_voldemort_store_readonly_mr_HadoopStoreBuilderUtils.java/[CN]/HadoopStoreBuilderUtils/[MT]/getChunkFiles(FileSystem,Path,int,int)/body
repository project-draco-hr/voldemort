{
  return fs.listStatus(path,new PathFilter(){
    public boolean accept(    Path input){
      if (input.getName().startsWith(Integer.toString(partitionId) + "_" + Integer.toString(replicaType))) {
        return true;
      }
 else {
        return false;
      }
    }
  }
);
}
