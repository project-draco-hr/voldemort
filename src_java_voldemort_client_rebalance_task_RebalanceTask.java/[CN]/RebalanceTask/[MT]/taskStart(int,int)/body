{
  taskCompletionTimeMs=System.currentTimeMillis();
  taskLog("[TaskId : " + taskId + " ] Starting rebalance of "+ partitionStoreCount+ " partition-stores for async operation id "+ rebalanceAsyncId+ ".");
  progressBar.beginTask(taskId);
}
