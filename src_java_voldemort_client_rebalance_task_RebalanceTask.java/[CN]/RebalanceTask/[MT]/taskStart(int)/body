{
  taskCompletionTimeMs=System.currentTimeMillis();
  taskLog("Starting rebalance of " + partitionStoreCount + " partition-stores for async operation id "+ rebalanceAsyncId+ ".");
  progressBar.beginTask(taskId);
}
