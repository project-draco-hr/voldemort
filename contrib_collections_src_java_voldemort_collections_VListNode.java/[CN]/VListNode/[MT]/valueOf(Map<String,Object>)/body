{
  @SuppressWarnings("unchecked") T value=(T)map.get(VALUE);
  return new VListNode<T>(value,(Integer)map.get(ID),(Integer)map.get(PREVIOUS_ID),(Integer)map.get(NEXT_ID),(Boolean)map.get(STABLE));
}
