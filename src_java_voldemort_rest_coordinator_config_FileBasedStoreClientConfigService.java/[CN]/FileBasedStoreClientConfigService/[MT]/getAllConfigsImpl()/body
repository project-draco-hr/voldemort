{
  try {
    return Joiner.on("\n").join(IOUtils.readLines(new FileReader(new File(coordinatorConfig.getFatClientConfigPath())))).trim();
  }
 catch (  IOException e) {
    logger.error("Problem reading the config file:\n",e);
    throw new RuntimeException("Problem reading the config file",e);
  }
}
