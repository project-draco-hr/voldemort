{
  super(ServiceType.SOCKET);
  this.server=new SocketServer(serviceName,port,coreConnections,maxConnections,socketBufferSize,requestHandler);
  this.serviceName=serviceName;
  this.enableJmx=enableJmx;
}
