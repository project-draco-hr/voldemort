{
  super(ServiceType.SOCKET);
  this.server=new SocketServer(port,coreConnections,maxConnections,socketBufferSize,requestHandlerFactory);
  this.serviceName=serviceName;
  this.enableJmx=enableJmx;
}
