{
  super(ServiceType.SOCKET);
  this.server=new SocketServer(serviceName,port,coreConnections,maxConnections,socketBufferSize,requestHandlerFactory);
  this.serviceName=serviceName;
  this.enableJmx=enableJmx;
}
