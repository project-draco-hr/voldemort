{
  responseBuilder.setError(ProtoUtils.encodeError(errorCodeMapper,e));
  if (logger.isEnabledFor(Level.ERROR))   logger.error(getHandlerName() + " handleUpdatePartitionEntries failed for request(" + request+ ")",e);
}
