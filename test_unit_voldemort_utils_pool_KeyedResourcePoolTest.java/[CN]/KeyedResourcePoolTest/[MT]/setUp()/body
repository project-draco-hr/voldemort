{
  factory=new TestResourceFactory();
  ResourcePoolConfig config=new ResourcePoolConfig().setMaxPoolSize(POOL_SIZE).setTimeout(TIMEOUT_MS,TimeUnit.MILLISECONDS).setMaxInvalidAttempts(MAX_ATTEMPTS);
  this.pool=new KeyedResourcePool<String,TestResource>(factory,config);
}
