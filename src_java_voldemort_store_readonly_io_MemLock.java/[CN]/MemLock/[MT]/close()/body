{
  if (logger.isDebugEnabled())   logger.debug("munlocking " + file + " with length "+ length);
  if (pa != null) {
    Pointer xpa=pa;
    pa=null;
    mman.munlock(xpa,length);
    mman.munmap(xpa,length);
  }
}
