{
  HttpURLConnection conn=null;
  conn=doDelete(urlStr,key,storeNameStr,String.valueOf(originTime),String.valueOf(timeOut),String.valueOf(routingType),eTag);
  conn.getResponseCode();
  conn.disconnect();
}
