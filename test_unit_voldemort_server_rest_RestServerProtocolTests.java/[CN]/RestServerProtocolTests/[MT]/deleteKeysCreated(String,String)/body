{
  HttpURLConnection conn=null;
  conn=doDelete(urlStr,key,storeNameStr,String.valueOf(System.currentTimeMillis()),String.valueOf(timeOut),String.valueOf(routingType),eTag);
  conn.getResponseCode();
  conn.disconnect();
}
