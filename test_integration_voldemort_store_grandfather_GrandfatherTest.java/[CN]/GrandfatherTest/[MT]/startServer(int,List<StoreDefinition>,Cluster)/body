{
  VoldemortConfig config=ServerTestUtils.createServerConfigWithDefs(false,nodeId,TestUtils.createTempDir().getAbsolutePath(),cluster,storeDef,new Properties());
  config.setGrandfather(true);
  config.setSlopFrequencyMs(10000);
  VoldemortServer server=new VoldemortServer(config);
  server.start();
  serverMap.put(nodeId,server);
  return server;
}
