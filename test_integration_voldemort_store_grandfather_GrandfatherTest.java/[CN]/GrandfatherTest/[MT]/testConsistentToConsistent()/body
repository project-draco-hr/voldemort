{
  StoreDefinition storeDefRepFactor=ServerTestUtils.getStoreDef("test1",1,1,1,1,1,RoutingStrategyType.CONSISTENT_STRATEGY);
  test(consistentRoutingCluster,consistentRoutingClusterModified,Lists.newArrayList(storeDefRepFactor),Lists.newArrayList(storeDefRepFactor));
  StoreDefinition storeDefRepFactor2=ServerTestUtils.getStoreDef("test1",2,2,2,2,2,RoutingStrategyType.CONSISTENT_STRATEGY);
  test(consistentRoutingCluster,consistentRoutingClusterModified,Lists.newArrayList(storeDefRepFactor2),Lists.newArrayList(storeDefRepFactor2));
}
