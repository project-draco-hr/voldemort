{
  String storeName=null;
  String[] parts=requestURI.split("/");
  if (parts.length > 1 && (this.fatClientMap.containsKey(parts[1]) || parts[1].equalsIgnoreCase(SCHEMATA))) {
    storeName=parts[1];
  }
  return storeName;
}
