{
  for (  int node : nodeToStart) {
    VoldemortServer server=ServerTestUtils.startVoldemortServer(ServerTestUtils.createServerConfig(node,TestUtils.createTempDir().getAbsolutePath(),null,getStoresXmlFile()),cluster);
    serverMap.put(node,server);
  }
  return cluster;
}
